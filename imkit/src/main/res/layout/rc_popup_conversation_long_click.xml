<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content">

    <!-- 三角形指示器 -->
    <ImageView
        android:id="@+id/rc_popup_triangle_indicator"
        android:layout_width="16dp"
        android:layout_height="7dp"
        android:layout_gravity="bottom|start"
        android:scaleType="fitXY"
        android:src="@drawable/rc_lively_primary_triangle_background"
        android:visibility="gone" />

    <!-- 主内容容器 -->
    <LinearLayout
        android:id="@+id/rc_popup_content_container"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="@drawable/rc_lively_common_background_radius_8"
        android:orientation="horizontal"
        android:paddingStart="10dp"
        android:paddingEnd="10dp"
        android:paddingTop="8dp"
        android:paddingBottom="8dp">

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/rc_recycler_conversation_long_click"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:overScrollMode="never" />
    </LinearLayout>
</FrameLayout>

